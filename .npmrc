# Hoist dependencies to match npm-style flat node_modules layout.
# Required because some packages (e.g. vite plugins) don't declare all their
# peer dependencies and fail under pnpm's default strict isolation.
shamefully-hoist=true
